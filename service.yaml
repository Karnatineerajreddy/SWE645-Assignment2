apiVersion: v1
kind: Service
metadata:
  name: student-survey-service
spec:
  selector:
    app: student-survey
  ports:
    - protocol: TCP
      port: 80        # Exposed on external LB
      targetPort: 80  # Container port
  type: LoadBalance